/*
 * 实现功能
 *
 * 1. 使能串口，将接收到的串口数据写入到文件中
 * 2. 启用两个线程，线程1负责获取串口数据，线程2负责定时将
*/

#include <iostream>
#include <string>
#include <vector>

#include "serial/serial.h"

int main()
{
    std::string port;
    uint32_t baud;

    return 0;
}

